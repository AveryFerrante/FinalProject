#include "hashtable.h"

HashTable::HashTable(unsigned int tableSize)
{
    this->tablesize = tableSize;
}

int HashTable::hash(std::string key){
    int index = 0;
    //Just an idea

    return index;
}
